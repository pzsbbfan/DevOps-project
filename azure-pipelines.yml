trigger:
  branches:
    include:
      - main

pool:
  name: Default  # Use your configured agent pool

steps:
# Step 1: Checkout Code
- checkout: self
  displayName: 'Checkout code from GitHub'

# Step 2: Debugging
- script: |
    echo "Testing minimal pipeline"
  displayName: 'Test Pipeline'
